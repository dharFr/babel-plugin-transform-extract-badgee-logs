import badgee from 'badgee';

badgee.log(___badgee[0]);
badgee.log(___badgee[1], ___badgee[2], ___badgee[3], hey[0]);
const logger = badgee.get('logger');

function foo() {
  logger.log(___badgee[3]);
  blah();
}